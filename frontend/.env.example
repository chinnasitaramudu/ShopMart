# Purpose: Frontend runtime configuration template for API connectivity.
# API integration: src/services/apiClient.js reads this base URL and routes all frontend requests to backend /api endpoints.
VITE_API_BASE_URL=http://localhost:5000/api
