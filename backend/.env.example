# Purpose: Backend runtime configuration template for ShopSmart.
# Database connection: src/config/db.js reads MONGO_URI at server startup and opens one shared Mongoose connection.
# Authentication: JWT_SECRET and JWT_EXPIRE are used by src/utils/generateToken.js and src/middleware/authMiddleware.js.

NODE_ENV=development
PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/shopsmart
JWT_SECRET=replace_with_a_long_secure_secret
JWT_EXPIRE=7d
CLIENT_URL=http://localhost:5173
